// Please do not change the name of this function
const secondsToTimeString = (seconds) => {
  // get numbers

  //get singular or plural units

  //  if built string is not empty += ', INPUT'

  // if elements not 0 >1 and last element += ' and INPUT'  

  // consider object, 2 arrays
};

module.exports = { secondsToTimeString };
